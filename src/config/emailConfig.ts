// Web3Forms Configuration
// Get your free access key from https://web3forms.com/

export const EMAIL_CONFIG = {
  WEB3FORMS_ACCESS_KEY: import.meta.env.VITE_WEB3FORMS_ACCESS_KEY || '',
  FROM_NAME: import.meta.env.VITE_FROM_NAME || 'Velvet Tasks',
};

// Setup Instructions:
// 1. Copy .env.example to .env
// 2. Go to https://web3forms.com/
// 3. Enter your email to get a free access key (no account needed)
// 4. Add your access key to the .env file
// 5. Restart the dev server
